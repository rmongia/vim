vim
===

RD's vim directory

Install Instructions
====================

git submodule init
git submodule update --recursive
cd bundle/YouCompleteMe
git checkout master
git submodule init
git submodule update --recursive
./install.sh --clang-completer

